<button type="button" class="btn btn-sm link-icon header-button" data-toggle="modal" data-target="#emailRecipeModal">
  <%= octicon "mail" %>
</button>
<div class="modal fade" id="emailRecipeModal" tabindex="-1" role="dialog" aria-labelledby="emailRecipeModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="emailRecipeModalLabel">Email this Recipe</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
         <%= form_tag send_email_recipe_path(@recipe.id), remote: true do  %>
          <div class="form-group">
            <%= label_tag :email %>
            <%= email_field_tag :email, nil, class: "form-control", required: true %>
          </div>
          <div class="form-group">
            <button type="button" class="btn btn-secondary" id="email-close-button" data-dismiss="modal">Close</button>
            <%= submit_tag 'Send', class: "btn btn-secondary" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>